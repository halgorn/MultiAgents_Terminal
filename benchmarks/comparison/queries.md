# Default Query Set — Aion Comparison Benchmark
# Lines starting with # are ignored. Only lines that look like queries are loaded.

How does authentication work?
Where is the CLI command surface defined?
What are the public exports of this project?
Which files handle MCP protocol communication?
Find all error handling patterns

Where is the test runner entry point?
List all files that import from 'zod'
What functions handle git operations?
Find all schema definitions

Explain the agent pipeline state machine
How does the MCP server handle freshness signals?